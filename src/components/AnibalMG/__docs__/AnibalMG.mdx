import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as CustomAnibalMG from "./AnibalMG.stories.tsx";

<Meta of={CustomAnibalMG} title="CustomAnibalMG" />

# CustomAnibalMG

Componente de botón con diferentes especializaciones.

#### Example

<Canvas of={CustomAnibalMG.Primary} />

## Usage

```ts
import {CustomAnibalMG} from 'milibreria';

function Example(){
    return (
        <CustomAnibalMG
            text: "AnibalMG primary",
            txtcolor: 'black',
            bgcolor: 'red',
            disabled: false,
            size: "small",
            onClick: () => alert("AnibalMG Primary"),
        />
    );
};

export default Example;
```

#### Example

<Canvas of={CustomAnibalMG.Secondary} />

## Usage

```ts
import {CustomAnibalMG} from 'milibreria';

function Example(){
    return (
        <CustomAnibalMG
            text: "AnibalMG secondary",
            txtcolor: 'white',
            bgcolor: 'pink',
            disabled: false,
            size: "small",
            onClick: () => alert("AnibalMG Secondary"),
        />
    );
};